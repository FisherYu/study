
###4.2.1
不同于类语言（比如C++，Smalltalk，Java），ECMAScript不使用类创建对象。对象可以通过字面量或者构造器创建。

###4.3.6 原生对象（native object）
由ES标准定义语法的对象，不是宿主环境定义的语法。
注意：本文档定义的事标准的原生对象。有些原生对象事内置的（build-in），有些则可能是在ES程序执行过程中构建的（嘛意思）。

###4.3.7 内置对象（build-in）
由ES实现提供的对象，独立于宿主环境。

每个对象不是原生对象就是宿主对象

### 4.3.9 undefined value
undefined表示变量还没有赋值。

### 4.3.26 属性（property）
属性是一个名字和一个值的关联。属性值可以通过一个数据值直接表示，或者通过访问函数（gettter，setter）间接表示

### 4.3.29 特性（attribute）
表示属性的特性的内部值。